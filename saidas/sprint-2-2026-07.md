# Sprint 2 — próximo trabalho, na ordem de execução

> Montado 10/07/2026 (Lucio + IA), no fim da oficina do agente. Pronto pra pegar e executar.
> Sprint 1 (view mobile do agente + F1/F2) **concluído e no ar** — ver [`oficina-agente-2026-07.md`](oficina-agente-2026-07.md).
> Fluxo de cada item: **branch → preview Vercel → verificar → promover** (provado na sessão 10/07).
> Contexto: roadmap [`roadmap-plataforma-unica-2026-07.md`](roadmap-plataforma-unica-2026-07.md) · regra [`regra-negocio-parceiro-2026-07.md`](regra-negocio-parceiro-2026-07.md).

## Ordem

### 1. P3 — Teste do link 77,70 → PIX  *(fecha a Parte 3 da oficina)*
O "teste do operador" que ficou pendente. Independente, rápido (~5 min), alta confiança.
- Abrir o **link privilegiado** (`/oferta-parceiro?k=...`) → cadastro enxuto → **"Ativar agora — R$77,70"** → deve cair no modal do CPF → checkout Asaas abrindo em **R$77,70** (não 147). Cronometrar QR→PIX (meta ≤2 min).
- *(Opcional, ciclo completo)* pagar → conferir empresa `approved` +6 meses no admin → estornar (webhook revoga sozinho).
- Roteiro detalhado: [`teste-e-limpeza-fluxo-77.md`](teste-e-limpeza-fluxo-77.md). **Não completar pagamento real sem o Lucio.**
- Tamanho: **P**. Depende de: nada.

### 2. Quick wins  *(baratos, alto valor, cabem numa branch só)*
- **Alinhar status agente × admin** — o dropdown do agente tem 7 status (inclui *Qualificação*); o kanban admin só 6 (`STATUS_COLUMNS` sem `qualificacao`). Lead em "Qualificação" **some** do kanban. Escolher: add coluna no admin OU tirar do agente. (`AgentLeads.tsx` × `admin/crm/Leads.tsx`)
- **Fix de copy** — upsell do Mini CRM diz "plano Premium"; o ativo é "Autoridade". 1 linha. (`MiniCrm.tsx`)
- **Selo Verificado na Presença** — ressignificar como "negócio real, visitado/confirmado" (a visita do agente É a verificação). 2 selos: ✅ Verificado × 👑 Autoridade. Via padrão `plans`/`useCompanyPlan`.
- Tamanho: **P–M**. Depende de: nada.

### 3. Fase 0 ⭐ — Fechar o ciclo de cobrança no CRM + tag de unidade  *(keystone do roadmap)*
Destrava LTV, comissão e carteira. Maior peso técnico do sprint.
- `asaas-webhook/index.ts`: além de `companies`, **escrever** `crm_customers` + `crm_subscriptions` + `crm_invoices` (invoice paga → confirmed, `paid_at`, `payment_method`).
- `externalReference` carrega **unidade** (`company_id|plan_slug|offer|unit`).
- Migration: coluna `unit` (enum `rede_baixada|rede_publicidade`) em invoices/subscriptions/customers.
- Verificar: cobrança sandbox → pagar → conferir writes com `unit` certa (query REST) → estornar → conferir revogação.
- Tamanho: **M**. Depende de: nada (mas é pré-req das fases seguintes).

### 4. Fase 1 — Carteira/LTV (admin) + "Meus Serviços" (cliente)
- Admin: ligar UI de `crm_subscriptions`/`crm_invoices` (schema-only) → carteira + receita por unidade.
- Cliente: painel **"Meus Serviços / Histórico"** + CTA "contratar mais / falar com a equipe".
- Tamanho: **M**. Depende de: **Fase 0**.

### 5+. Sequência do roadmap
- **Fase 2** — GMN/Site/Movimento como produtos vendáveis no mesmo ledger. *(dep: Fase 1)*
- **Fase 3** — comissão automática + painel do representante. *(dep: Fase 0)*
- **Fase 4** — claim/ghost ("esta empresa é sua?"). *(paralelo — pode rodar a qualquer momento)*
- **Fase 5** — chamados/OS + entrega (briefings/propostas). *(por último, mais pesada)*
- Detalhe completo: [`roadmap-plataforma-unica-2026-07.md`](roadmap-plataforma-unica-2026-07.md).

## Notas de execução
- Contas de teste: agente `elis@redepublicidade.com.br` (moderator) · admin `redebaixada@gmail.com` (super_admin) — senha `123456`. Dado de teste sempre marcado **"TESTE Claude"** + limpar no fim.
- Ao automatizar: form controlado do login e Radix Select **não** commitam por clique sintético — usar setter nativo / teclado.
